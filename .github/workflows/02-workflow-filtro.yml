name: 02 - Workflow filtro

on: 
  push:
    branches: feature/**

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Exibir o Evento
        run: echo "workflow disparar por ${{github.event_name}}"